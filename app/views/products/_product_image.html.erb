<% if product.cover.attached? %>
  <%= image_tag product.cover, style: "border-radius: 4px; box-shadow: rgba(0, 0, 0, 0.24) 0px 3px 8px;" %>
<% else %>
  <%= image_tag "no_image.png", size:"150x150" %>
<%end%>
